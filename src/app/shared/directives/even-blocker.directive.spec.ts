import { EvenBlockerDirective } from './even-blocker.directive';

describe('EvenBlockerDirective', () => {
  it('should create an instance', () => {
    const directive = new EvenBlockerDirective();
    expect(directive).toBeTruthy();
  });
});
